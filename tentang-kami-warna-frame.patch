diff --git a/resources/views/pages/admin/edit-web.blade.php b/resources/views/pages/admin/edit-web.blade.php
--- a/resources/views/pages/admin/edit-web.blade.php
+++ b/resources/views/pages/admin/edit-web.blade.php
@@ -78,6 +78,28 @@
 
     public ?string $profilHeroFotoPathLama = null;
 
+    /** Sama pola & default-nya dengan $headerUseCustomBg / $headerBgColor -- lihat komentar di sana. */
+    public bool $profilHeroUseCustomBg = false;
+
+    public string $profilHeroBgColor = '#F4EDE0';
+
+    /**
+     * Preset warna frame foto Tentang Kami. Sama daftarnya dengan
+     * $headerBgPresets supaya konsisten dengan section Header Beranda.
+     *
+     * @var array<int, array{label: string, value: string, check: string}>
+     */
+    public array $profilHeroBgPresets = [
+        ['label' => 'Krem Hangat (Default)', 'value' => '#F4EDE0', 'check' => '#3D2B1F'],
+        ['label' => 'Putih Gading', 'value' => '#FFFDF8', 'check' => '#3D2B1F'],
+        ['label' => 'Beige Pasir', 'value' => '#EFE3D0', 'check' => '#3D2B1F'],
+        ['label' => 'Sage Hijau Lembut', 'value' => '#DCE3CE', 'check' => '#3D2B1F'],
+        ['label' => 'Terracotta', 'value' => '#C97B5A', 'check' => '#FFFFFF'],
+        ['label' => 'Coklat Kayu Tua', 'value' => '#3D2B1F', 'check' => '#FFFFFF'],
+        ['label' => 'Navy Malam', 'value' => '#22303F', 'check' => '#FFFFFF'],
+        ['label' => 'Hitam Elegan', 'value' => '#1A1A1A', 'check' => '#FFFFFF'],
+    ];
+
     /**
      * Halaman apa saja yang bisa dikelola dari "Edit Web", ditampilkan
      * sebagai KARTU PILIHAN dulu (bukan langsung sidebar tab). Klik kartu
@@ -517,6 +539,7 @@
     private function profilHeroDefaults(): array
     {
         return [
+            'bg_color' => null,
             'eyebrow' => 'Tentang Kami',
             'heading_line1' => 'Mewujudkan Ruang',
             'heading_line2' => 'yang Punya Cerita.',
@@ -612,6 +635,8 @@
         $this->profilHeroHeadingLine2 = $profilHeroData['heading_line2'];
         $this->profilHeroDescription = $profilHeroData['description'];
         $this->profilHeroFotoPathLama = $profilHeroData['image_path'];
+        $this->profilHeroUseCustomBg = filled($profilHeroData['bg_color']);
+        $this->profilHeroBgColor = $profilHeroData['bg_color'] ?: $this->profilHeroBgColor;
     }
 
     public function selectSection(string $key): void
@@ -660,6 +685,12 @@
         $this->testimoniCardColor[$rank] = $hex;
     }
 
+    public function selectProfilHeroBgPreset(string $hex): void
+    {
+        $this->profilHeroUseCustomBg = true;
+        $this->profilHeroBgColor = $hex;
+    }
+
     /**
      * Foto kolase besar (kiri, rasio 3:4). Sebelum admin pernah mengganti,
      * pakai foto stok Pexels yang sama dengan yang tampil di Beranda --
@@ -1071,6 +1102,7 @@
     public function saveProfilHero(): void
     {
         $validated = $this->validate([
+            'profilHeroBgColor' => ['required', 'string', 'regex:/^#[0-9A-Fa-f]{6}$/'],
             'profilHeroEyebrow' => ['required', 'string', 'max:40'],
             'profilHeroHeadingLine1' => ['required', 'string', 'max:60'],
             'profilHeroHeadingLine2' => ['required', 'string', 'max:60'],
@@ -1102,6 +1134,7 @@
 
         HomeSection::forSection('profil-toko')->update([
             'data' => [
+                'bg_color' => $this->profilHeroUseCustomBg ? $validated['profilHeroBgColor'] : null,
                 'eyebrow' => $validated['profilHeroEyebrow'],
                 'heading_line1' => $validated['profilHeroHeadingLine1'],
                 'heading_line2' => $validated['profilHeroHeadingLine2'],
@@ -2355,6 +2388,59 @@
                         </template>
                     </div>
 
+                    {{--
+                        WARNA -- warna latar/frame kotak foto kanan section ini.
+                        Pola sama persis dengan blok Warna di section Header
+                        Beranda (headerUseCustomBg / headerBgColor).
+                    --}}
+                    <div class="space-y-4 rounded-xl border border-admin-border p-4">
+                        <p class="text-xs font-semibold uppercase tracking-wide text-admin-ink-soft">Warna</p>
+
+                        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
+                            <label class="flex items-center gap-2 text-sm text-admin-ink">
+                                <input type="checkbox" wire:model.live="profilHeroUseCustomBg" class="h-4 w-4 rounded border-admin-border text-admin-accent focus:ring-admin-accent">
+                                Pakai warna frame khusus
+                            </label>
+                            <input
+                                type="color" wire:model="profilHeroBgColor"
+                                @disabled(! $profilHeroUseCustomBg)
+                                class="h-10 w-16 cursor-pointer rounded-lg border border-admin-border disabled:cursor-not-allowed disabled:opacity-40"
+                            >
+                        </div>
+
+                        {{-- Preset warna rekomendasi -- klik langsung pakai warnanya + otomatis centang checkbox di atas. --}}
+                        <div>
+                            <p class="mb-2 text-[11px] font-medium uppercase tracking-wide text-admin-ink-soft">Atau pilih dari rekomendasi</p>
+                            <div class="flex flex-wrap gap-2.5">
+                                @foreach ($profilHeroBgPresets as $preset)
+                                    <button
+                                        type="button"
+                                        wire:click="selectProfilHeroBgPreset('{{ $preset['value'] }}')"
+                                        title="{{ $preset['label'] }}"
+                                        class="group flex flex-col items-center gap-1"
+                                    >
+                                        <span
+                                            class="flex h-9 w-9 items-center justify-center rounded-full shadow-sm ring-1 ring-inset ring-white/40 transition duration-200 group-hover:scale-110 group-hover:shadow-md
+                                            {{ $profilHeroUseCustomBg && strtoupper($profilHeroBgColor) === $preset['value']
+                                                ? 'border-2 border-admin-accent ring-2 ring-admin-accent ring-offset-2 ring-offset-admin-surface'
+                                                : 'border border-admin-border group-hover:border-admin-accent/60' }}"
+                                            style="background: linear-gradient(135deg, color-mix(in oklab, {{ $preset['value'] }} 100%, white 30%), {{ $preset['value'] }} 55%, color-mix(in oklab, {{ $preset['value'] }} 100%, black 16%));"
+                                        >
+                                            @if ($profilHeroUseCustomBg && strtoupper($profilHeroBgColor) === $preset['value'])
+                                                <i class="fa-solid fa-check text-xs drop-shadow-sm" style="color: {{ $preset['check'] }};"></i>
+                                            @endif
+                                        </span>
+                                        <span class="max-w-14 truncate text-[10px] text-admin-ink-soft">{{ $preset['label'] }}</span>
+                                    </button>
+                                @endforeach
+                            </div>
+                        </div>
+
+                        <p class="text-xs text-admin-ink-soft">
+                            Kalau tidak dicentang, frame foto ikut warna default (krem hangat).
+                        </p>
+                    </div>
+
                     {{-- ISI TEKS --}}
                     <div class="space-y-4 rounded-xl border border-admin-border p-4">
                         <p class="text-xs font-semibold uppercase tracking-wide text-admin-ink-soft">Isi Teks</p>
diff --git a/resources/views/pages/frontend/profil.blade.php b/resources/views/pages/frontend/profil.blade.php
--- a/resources/views/pages/frontend/profil.blade.php
+++ b/resources/views/pages/frontend/profil.blade.php
@@ -34,6 +34,7 @@
         // TIDAK diedit di sini (link & tulisan tetap, sama seperti tombol
         // CTA Header Beranda).
         $profilHero = \App\Models\HomeSection::dataFor('profil-toko', [
+            'bg_color' => null,
             'eyebrow' => 'Tentang Kami',
             'heading_line1' => 'Mewujudkan Ruang',
             'heading_line2' => 'yang Punya Cerita.',
@@ -44,6 +45,10 @@
         $profilHeroImageUrl = $profilHero['image_path']
             ? \Illuminate\Support\Facades\Storage::disk('public')->url($profilHero['image_path'])
             : asset('images/admin-login/kursi.png');
+
+        // Kalau admin belum pernah pilih warna khusus, frame tetap pakai
+        // warna default krem hangat (sama dengan class bg-admin-cream lama).
+        $profilHeroBgColor = $profilHero['bg_color'] ?: '#F4EDE0';
     @endphp
 
     {{-- =====================================================
@@ -58,20 +63,17 @@
             <div data-reveal>
                 <div class="flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.2em] text-admin-accent">
                     <span class="h-px w-8 bg-admin-accent"></span>
-                    Tentang Kami
+                    {{ $profilHero['eyebrow'] }}
                 </div>
 
                 <h1 class="mt-5 font-display text-4xl leading-[1.12] text-[#3D2B1F] sm:text-5xl lg:text-[3.25rem]">
-                    Mewujudkan Ruang
+                    {{ $profilHero['heading_line1'] }}
                     <br class="hidden sm:block">
-                    yang Punya Cerita.
+                    {{ $profilHero['heading_line2'] }}
                 </h1>
 
                 <p class="mt-6 max-w-md text-sm leading-relaxed text-[#6B6E76] sm:text-base">
-                    {{ $profileSetting->site_name }} menghadirkan furnitur yang dibuat dengan
-                    teliti untuk melengkapi ruang Anda — bukan sekadar mengisinya. Setiap
-                    karya dirancang untuk nyaman digunakan sekaligus enak dipandang, untuk
-                    rumah maupun ruang kerja.
+                    {{ $profilHero['description'] }}
                 </p>
 
                 <div class="mt-8 flex flex-wrap items-center gap-3">
@@ -94,9 +96,9 @@
             </div>
 
             <div class="relative flex items-center justify-center" data-reveal style="transition-delay:.1s">
-                <span class="relative flex aspect-10/9 w-full items-center justify-center overflow-hidden rounded-[28px] bg-admin-cream shadow-xl shadow-black/10">
+                <span class="relative flex aspect-10/9 w-full items-center justify-center overflow-hidden rounded-[28px] shadow-xl shadow-black/10" style="background-color: {{ $profilHeroBgColor }};">
                     <img
-                        src="{{ asset('images/admin-login/kursi.png') }}"
+                        src="{{ $profilHeroImageUrl }}"
                         alt="Furniture {{ $profileSetting->site_name }}"
                         class="h-[82%] w-auto object-contain drop-shadow-2xl"
                     >
